spring:
  application:
    name: user
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://localhost:8888}
      fail-fast: true
      username: ${CONFIG_AUTH_NAME:root}
      password: ${CONFIG_AUTH_PASSWORD:s3cr3t_pass}
